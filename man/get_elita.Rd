% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_elita.R
\name{get_elita}
\alias{get_elita}
\title{Get ELIta-family Lexicons}
\usage{
get_elita(dict = "elita_VAD", rescale = "default")
}
\arguments{
\item{dict}{The name of the lexicon to retrieve. Must be one of:
\code{"elita_VAD"} (default) or \code{"elita_basic"}.}

\item{rescale}{Character string indicating the rescaling method applied to
the scores. Options are:
\itemize{
\item{\code{"default"}: For \code{elita_VAD} = \code{"centered"}. For \code{elita_basic} = \code{"none"}.}
\item{\code{"none"}: No rescaling is applied.}
\item{\code{"centered"}: Scores are divided by their theoretical maximum (4 for \code{elita_VAD}) to map to -1/+1.}
\item{\code{"normalized"}: Scores are normalized empirically to -1/+1 (\code{score / max(abs(.x), na.rm = TRUE)}).}
}
This argument only applies \code{elita_VAD}. \code{elita_basic} scores are within
0,1,}
}
\value{
A \code{tibble}.
}
\description{
A utility function to access ELIta-family lexicons (\code{\link{elita_basic}},
\code{\link{elita_VAD}}) with convenient defaults.

For all lexicons, it returns the key entry (\code{lemma} or \code{word}) and \code{score}
columns, suitable for joining.

For \code{elita_VAD}, which contains scores on a -4 to +4 scale, scores are
"centered" by default (divided by 4 to map to a -1 to +1 theoretical range).
}
\examples{
\dontrun{
# Get the default elita_VAD lexicon (centered scores)
my_dict_VAD <- get_elita("elita_VAD")

# Get elita_VAD without any rescaling
my_dict_VAD <- get_elita("elita_VAD", rescale = "none")

# Get elita_basic lexicon
my_dict_basic <- get_elita("elita_basic")
}
}
\seealso{
\code{\link{elita_VAD}}, \code{\link{elita_basic}}, \code{\link[=get_sentix]{get_sentix()}}
}
\keyword{functions}
