% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentix_annotate.R
\name{sentix_annotate}
\alias{sentix_annotate}
\title{Annotate text with sentiment scores}
\usage{
sentix_annotate(
  x,
  model = NULL,
  text_field = NULL,
  docid_field = NULL,
  dict = "sentix",
  rescale = "default",
  simplify = TRUE,
  ...
)
}
\arguments{
\item{x}{A character vector, a data frame, or a list of texts.}

\item{model}{A UDpipe model used for annotation. The following options are
supported:
\itemize{
\item{\code{NULL} (default): The function will automatically download and
load the default Italian model (\code{italian-isdt}). Requires an internet
connection on first use.}
\item{\code{"local"}: Searches for a single \code{.udpipe} model file in the
current working directory.}
\item{A character string providing the full path to a \code{.udpipe} file
("path/to/model").}
\item{A model object already loaded with
\code{\link[udpipe]{udpipe_load_model}}. This is the most efficient
option for repeated calls.}
}}

\item{text_field}{A character string specifying the name of the column
containing the text to be parsed.
If \code{NULL} (the default), the function will try to detect column named
"text".}

\item{docid_field}{A character string specifying the name of the column
containing document identifiers. If \code{NULL} (the default), the function
will try to detect a column named "doc_id"; if not found,
IDs will be automatically generated.}

\item{dict}{The name of the lexicon to use. Can be one of the Sentix family
(\code{"sentix"}, \code{"MAL"}) or ELIta family (\code{"elita_VAD"},
\code{"elita_basic"}). Defaults to \code{"sentix"}.}

\item{rescale}{Character string indicating the rescaling method for scores:
\code{"default"} (for the lexicon), \code{"none"}, \code{"centered"}, \code{"normalized"}.
Defaults to \code{"default"}.
Passed to \code{\link[=get_elita]{get_elita()}}.}

\item{simplify}{Logical. Defaults to \code{TRUE}. If \code{FALSE}, returns standard
UDpipe columns (see Details).}

\item{...}{Additional arguments passed to
the lexicon retrieval function (see \code{\link[=get_sentix]{get_sentix()}}, \code{\link[=get_elita]{get_elita()}}).
OR to \code{\link[udpipe]{udpipe}} and subsequently to
\code{\link[udpipe]{udpipe_annotate}}.
Note: \code{tagger = "none"} is not supported (it will be ignored),
as POS tagging is required for lemmatization and punctuation filtering.}
}
\value{
A \code{tibble} with one row per token, containing the following
columns: \code{doc_id}, \code{sentence_id}, \code{token_id}, \code{token}, \code{lemma}, \code{upos}, one
or more sentiment score columns, named after those of the selected lexicon.

When \code{simplify = FALSE} it will include standard UDpipe columns (see
\code{\link[udpipe]{as.data.frame.udpipe_connlu}}), plus sentiment score
columns.
}
\description{
Annotates a character vector or a data frame of texts using
\code{\link[udpipe]{udpipe}} (providing
a model where necessary) and joins the results with a selected sentiment
lexicon.
}
\details{
This function uses \code{\link[udpipe]{udpipe}} to process the
input texts, then joins the tokenized output with the specified lexicon, using
\emph{dplyr} \code{\link[dplyr]{join}}. It performs two main steps:
\enumerate{
\item \strong{Parsing:} Texts are tokenized, tagged, and lemmatized. For
larger corpora, the function supports parallel processing by passing
the argument \code{parallel.cores} to \code{\link[udpipe]{udpipe}}.
\item \strong{Matching:} The parsed tokens are joined with the selected
lexicon using \code{\link[dplyr]{left_join}}.
}
All tokens are thus preserved in the output to maintain context, with
\code{NA} assigned to tokens not found in the lexicon.
}
\examples{
\dontrun{
# Auto-download model
ann_df <- sentix_annotate("Oggi è una bella giornata")

# Use a local model file in the working directory (i.e. if already
# downloaded)
ann_df <- sentix_annotate("Uso un modello locale.", model = "local")

# Use specific model path and lexicon
ann_df <- sentix_annotate("Oggi è una bella giornata",
    model = "path/to/model.udpipe",
    dict = "elita_VAD")

# With a data frame, and a loaded model
data("recensioni_tv")
model <- udpipe::udpipe_load_model("italian-isdt-ud-2.5-191206.udpipe")
ann_df <- sentix_annotate(recensioni_tv, model = model)
)
}
}
\seealso{
\code{\link[=get_sentix]{get_sentix()}}, \code{\link[=get_elita]{get_elita()}}, \code{\link[=sentix_summarize]{sentix_summarize()}}
}
\keyword{functions}
