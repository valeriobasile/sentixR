% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_polarity.R
\name{make_polarity}
\alias{make_polarity}
\title{Classify sentiment scores into polarity categories}
\usage{
make_polarity(score, threshold = 0)
}
\arguments{
\item{score}{A numeric vector of sentiment scores.}

\item{threshold}{A numeric vector.
If length 1 (i.e. \verb{threshold = 0.125)}), it must be a positive value,
that will be used as absolute
threshold; if length 2 (i.e. \code{threshold = c(0.125, -0.135)}), it must
contain one positive (or 0) and one negative (or 0) value.
Defaults to 0.

Scores \verb{>= threshold} and \verb{<= -threshold} will be classified as "positive"
and "negative", respectively. 0 is always classified as "neutral".}
}
\value{
A character vector with "positive", "negative", or "neutral".
}
\description{
Utility function for adding polarity columns to sentiment lexicons, to be used
within \code{\link[dplyr]{mutate}}.

Classifies numeric sentiment scores into "positive", "negative", or "neutral",
based on a specified threshold (defaults to 0).
}
\examples{
\dontrun{
library(dplyr)
sentix \%>\% 
  mutate(polarity = make_polarity(score))

# with custom threshold
elita_VAD \%>\%
  mutate(across(where(is.numeric), 
                      ~ make_polarity(.x, 0.125)))
  
# with custom asymmetric thresholds
get_sentix("MAL") \%>\% 
 mutate(polarity = make_polarity(score, 
                                 threshold = c(0.125, -0.135)))

# with the results of sentix_annotate
sentix_annotate(recensioni_tv, model = "local")  \%>\% 
 mutate(polarity = make_polarity(score, .125)) 
}
}
\keyword{functions}
