% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/df_to_dict.R
\name{df_to_dict}
\alias{df_to_dict}
\title{Convert a data frame to a Quanteda dictionary with polarity or valence}
\usage{
df_to_dict(
  x,
  word_field = NULL,
  type = "auto",
  polar_field = "polarity",
  polar_map = NULL
)
}
\arguments{
\item{x}{A \code{data.frame} or \code{tibble} with at least one character
column with the terms, and either numeric columns (scores) or a
categorical column (with polarity).}

\item{word_field}{A string with the name of the column containing the terms.
If \code{NULL} (the default), the function will try to
detect a column named "lemma" or "word".
If neither is found, it selects the first character column available.}

\item{type}{The type of dictionary to create. Can be \code{"auto"} (the default),
\code{"valence"}, or \code{"polarity"}.}

\item{polar_field}{A string with the name of the column containing the
categories (polarities; i.e. "Positive", "Negative").
Defaults to \code{"polarity"}. Ignored for valence dictionaries.}

\item{polar_map}{A named character vector to manually map dictionary keys
to standard polarity values (\code{pos}, \code{neg}, \code{neut}). Example:
\code{c(pos = "Positive", neg = "Negative")}. If \code{NULL} (default), the
function will attempt to auto-detect the mapping.}
}
\value{
A \code{quanteda::dictionary2} object.
}
\description{
Converts a data frame (tibble) containing a lexicon into a Quanteda dictionary
with valence or polarity.
Requires the package \emph{Quanteda}. If the \code{quanteda.sentiment} package is also
installed,
the polarity or valence attributes will be detected and assigned automatically.
Otherwise,
a standard Quanteda dictionary will be created.

The function is a wrapper for \code{\link[=df_to_valence]{df_to_valence()}} and \code{\link[=df_to_polar]{df_to_polar()}},
automatically determining, where possible,
the most appropriate type of dictionary for the input
data frame (see Details).

\strong{Note:} The function cannot handle duplicate entries,
and will remove rows with NAs.
}
\details{
The function handles the sentiment scores or categories as follows:
\itemize{
\item \strong{Valence Dictionaries}: The names of the numeric columns are used
as dictionary keys. When there is only one
numeric column, the \code{word_field} is used as the key name
(see \code{quanteda.sentiment::valence} if installed).
\item \strong{Polarity Dictionaries}:
\itemize{
\item The character or factor column (other
than the \code{word_field}) is used to group terms into the categories
(\code{polar_field}) that are then associated with the standard
"polarity" attribute ("\code{pos}", "\code{neg}", optionally "\code{neut}";
see \code{quanteda.sentiment::polarity} if installed).
\item The "polarity" attribute is assigned via the \code{polar_map} argument, or
automatically if the categories in the \code{polar_field} are explicit:
"positive", "negative" (and, optionally, "neutral"; case-insensitive).
}
}
}
\examples{
if(requireNamespace("quanteda")){
# only numeric fields are present
my_dict <- get_sentix()
df_to_dict(my_dict)

# no numeric fields are present
my_dict <- get_sentix(polarity = TRUE)
df_to_dict(my_dict)
}
}
\seealso{
\code{\link[=df_to_dict]{df_to_dict()}}, \code{\link[=df_to_polar]{df_to_polar()}},
\code{\link[quanteda]{dictionary}}
}
\keyword{functions}
