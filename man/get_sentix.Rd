% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sentix.R
\name{get_sentix}
\alias{get_sentix}
\title{Get Sentix-family Lexicons}
\usage{
get_sentix(
  dict = "sentix",
  polypathy = FALSE,
  polarity = FALSE,
  polar_field = "polarity",
  threshold = 0
)
}
\arguments{
\item{dict}{The name of the lexicon to retrieve. Must be one of: \code{"sentix"}
(default) or \code{"MAL"}.}

\item{polypathy}{Logical. If \code{TRUE}, the \code{polypathy_index} column
is included. Defaults to \code{FALSE}.}

\item{polarity}{Logical. If \code{TRUE}, a polarity column is added (computed
via \code{\link{make_polarity}}). Defaults to \code{FALSE}.}

\item{polar_field}{Character string. The name of the new polarity column.
Defaults to \code{"polarity"}.}

\item{threshold}{Numeric. The threshold for \code{\link{make_polarity}} (positive value).
Scores within \verb{[-threshold, threshold]} are considered neutral. Defaults to 0.}
}
\value{
A \code{tibble}.
}
\description{
A utility function to access Sentix-family lexicons (\code{\link{sentix}}, \code{\link{MAL}}
) with convenient defaults.

For all lexicons, it returns by default the key entry (\code{lemma} or \code{word}) and
\code{score}
columns, suitable for joining. Polarity classification can be computed via
\code{\link[=make_polarity]{make_polarity()}}.

Other columns (\code{polypathy_index}) are accessible via arguments.
}
\examples{
# Get the default sentix lexicon (key and score)
my_dict <- get_sentix()

# Get the sentix lexicon with polarity field
my_dict <- get_sentix(polarity = TRUE)

# Get MAL and polypathy index
my_dict_poly <- get_sentix("MAL", polypathy = TRUE)
}
\seealso{
\code{\link{sentix}}, \code{\link{MAL}}, \code{\link[=get_elita]{get_elita()}}, \code{\link[=make_polarity]{make_polarity()}}
}
\keyword{functions}
