% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentix_summarize.R
\name{sentix_summarize}
\alias{sentix_summarize}
\title{Summarize sentiment annotations}
\usage{
sentix_summarize(
  x,
  aggregation = "mean",
  cols = NULL,
  by = "doc_id",
  simplify = FALSE,
  ambiguity = "3"
)
}
\arguments{
\item{x}{A data frame containing at least a \code{doc_id} column and numeric
columns with sentiment scores.}

\item{aggregation}{Character. \code{"mean"} (default) or \code{"sum"}.}

\item{cols}{Character vector, specifying columns to summarize. If \code{NULL}
(default), numeric columns will be automatically considered as scores.}

\item{by}{Character vector, specifying the column(s) to group by. Defaults
to \code{doc_id}.}

\item{simplify}{Logical. Defaults to \code{FALSE}. When \code{TRUE}, the output will
contain only basic measures (\code{score} and, where applicable \code{ambiguity}).}

\item{ambiguity}{Character. The minimum \code{polypathy_index} value to
be considered. Can be \code{"1"}, \code{"2"}, \code{"3"} (default), or \code{"none"}
to disable ambiguity calculation (see Details).
Ignored if the column \code{polypathy_index} is not present in the input.}
}
\value{
A \code{tibble} with one row per document.
}
\description{
Calculates sentiment scores and, optionally, ambiguity metrics, aggregating
token-level sentiment annotations to the document level.
}
\details{
This function takes the output of \code{\link[=sentix_annotate]{sentix_annotate()}} or a data frame or
tibble withat least a \code{doc_id} column and sentiment scores (numeric columns).

\strong{Metrics Calculated:}
\itemize{
\item \code{score}: the average (or sum) of the sentiment columns.
\item \code{ambiguity}: \code{n_poly / n_scored} (if \code{polypathy_index} is present).
\item \code{n_tokens}: total valid tokens, excluding punctuation. UDpipe's CoNLL-U
format expands Multi-Word Tokens (MWTs) into their syntactic components,
including articulated prepositions: e.g., 'nella' becomes 'in' + 'la'.
The count only considers the components (e.g., 'nella' counts for 2
tokens, not 3).
\item \code{n_scored}: tokens with \emph{at least one} sentiment score.
\item \code{n_poly}: count of ambiguous tokens, based on the \code{ambiguity} level
setting, and if the column \code{polypathy_index} is present in the lexicon.
}
}
\examples{
\dontrun{

testo <- "Oggi è una bella giornata. Uscirò a fare una passeggiata"
# With the output of sentix_annotate
ann_df <- sentix_annotate(testo, model = "local")
sentix_summarize(ann_df)
# With only basic measures
sentix_summarize(ann_df, simplify = TRUE)
# With custom grouping (e.g., per sentence)
sentix_summarize(ann_df, by =c("doc_id", "sentence_id"))
# With the output of sentix_annotate, ambiguity and other intermediate 
# measures
ann_df <- sentix_annotate(testo, 
                          polypathy = TRUE,
                          model = "local")
sentix_summarize(ann_df)
}
}
\seealso{
\code{\link[=get_sentix]{get_sentix()}}, \code{\link{sentix}}, \code{\link[=sentix_annotate]{sentix_annotate()}}
}
\keyword{functions}
